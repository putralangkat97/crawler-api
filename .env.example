APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=app
DB_USERNAME=app
DB_PASSWORD=app

SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=redis

CACHE_STORE=redis
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

API_KEYS=comma,separated,keys

R2_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_REGION=auto
PRESIGN_TTL_SECONDS=600
TMP_OBJECT_TTL_HOURS=24

SCRAPE_HTTP_TIMEOUT_MS=8000
SCRAPE_CHROME_TIMEOUT_MS=20000
MAX_BYTES_DEFAULT=15000000

INLINE_TEXT_MAX_BYTES=131072

RATE_LIMIT_SCRAPE_PER_MIN=60
RATE_LIMIT_CRAWL_PER_MIN=5

MAX_SCRAPE_URLS_PER_REQUEST=10
MAX_CRAWL_PAGES_HARD_CAP=1000
MAX_CRAWL_RUNTIME_SECONDS=1800

RENDERER_BASE_URL=http://renderer:3001

VITE_APP_NAME="${APP_NAME}"

OCTANE_SERVER=frankenphp

SCRAPER_BLOCK_GIFS=true
SCRAPER_MAX_IMAGES=5
SCRAPER_CONCURRENCY=50
SCRAPER_PROMPT_FILTER_ENABLED=true

RENDERER_PORT=3001
RENDERER_MAX_CONCURRENCY=2
RENDERER_TIMEOUT_MS=20000
